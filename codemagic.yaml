
workflows:
  my-workflow:
    scripts:
      - name: Build Android APK
        working_directory: android
        script: ./gradlew assembleRelease
    artifacts:
      - TMessagesProj/app/build/outputs/**/**/*.apk
